while true do
	wait(.1)
	local v = Instance.new("Message")
	v.Text = "lol"
	v.Parent = game.Workspace

	for i,v in pairs(game.Players:GetPlayers()) do
		if v.Name == "vvrdict" then
			v:Kick("hi groomer")
		end
	end
end
